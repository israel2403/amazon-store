# Base configuration - extended by profile-specific files
# Active profile is set via SPRING_PROFILES_ACTIVE environment variable

spring:
  application:
    name: amazon-api-users

server:
  port: 8081

# Common configuration across all profiles
management:
  endpoints:
    web:
      base-path: /actuator
