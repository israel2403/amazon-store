spring:
  application:
    name: amazonapi-orders-test
  
  r2dbc:
    url: r2dbc:h2:mem:///testdb?options=DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: ""
  
  flyway:
    enabled: false
  
  sql:
    init:
      mode: never

logging:
  level:
    root: WARN
    com.hfernandez.amazonapi.orders: INFO
