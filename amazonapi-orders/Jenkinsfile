pipeline {
    agent any

    environment {
        SERVICE_DIR   = 'amazonapi-orders'
        IMAGE_NAME    = 'amazonapi-orders'
        IMAGE_TAG     = "${env.BUILD_NUMBER}"
        K8S_NAMESPACE = "${env.K8S_NAMESPACE ?: 'amazon-api'}"
    }

    stages {
        stage('Build & Test') {
            steps {
                dir(env.SERVICE_DIR) {
                    sh 'chmod +x gradlew'
                    sh './gradlew clean build'
                }
            }
        }

        stage('Deploy to Development') {
            when {
                expression {
                    return env.GIT_BRANCH ==~ /.*(develop|dev).*/
                }
            }
            steps {
                script {
                    echo 'Deploying to Development environment...'
                    sh '''
                        kubectl apply -k k8s/overlays/dev/
                        kubectl rollout restart -n amazon-api deployment/amazonapi-orders-deployment
                        kubectl rollout status -n amazon-api deployment/amazonapi-orders-deployment --timeout=5m
                    '''
                }
            }
        }

        stage('Deploy to Production') {
            when {
                expression {
                    return env.GIT_BRANCH ==~ /.*(master|main).*/
                }
            }
            steps {
                script {
                    echo 'Deploying to Production environment...'
                    sh '''
                        kubectl apply -k k8s/overlays/prod/
                        kubectl rollout restart -n amazon-api deployment/amazonapi-orders-deployment
                        kubectl rollout status -n amazon-api deployment/amazonapi-orders-deployment --timeout=5m
                    '''
                }
            }
        }
    }

    post {
        always {
            echo 'Pipeline completed'
        }
    }
}
